namespace StudentManagement.Enum
{
    public enum AttendanceStatus
    {
        Present = 1,      // Có mặt
        Absent = 2,       // Vắng mặt
        Late = 3,         // Đi muộn
        Excused = 4,      // Vắng có phép
        Left = 5          // Về sớm
    }
}